const authRouter = require('./auth');
const listingRouter = require('./listings');
const reviewRouter = require('./reviews');


module.exports = {
    authRouter,
    listingRouter,
    reviewRouter
}
